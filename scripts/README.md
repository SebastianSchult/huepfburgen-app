# Scripts

Project scripts should live here when they are too large or too specific for `package.json`.

Rules:

- Keep scripts deterministic.
- Avoid hardcoded secrets.
- Document required environment variables.
- Prefer npm scripts for common developer commands.

Current common commands are defined in `package.json`.

